<div class="container">
	<h2>List of Genres</h2>
	<table border="1" class="table">
		<tr>
			<th>Name<hd>
			<th>View Details</th>
		</tr>
		<% @genres.each do |genre| %>
			<tr>
				<td> <%= genre.Name %> </td>
				<td> <%= link_to 'Genre Page', genre_path(genre) %> </td>
			</tr>
		<% end %>
	</table>
</div>